#==========================================================================
# Description: Compile rockchip rk35xx kernel
# Copyright (C) 2023 https://github.com/unifreq/linux-5.10.y-rk35xx
# Copyright (C) 2023 https://github.com/ophub/kernel
#==========================================================================

name: Compile rockchip rk35xx kernel

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      kernel_source:
        description: "Select the kernel source"
        required: false
        default: "unifreq/linux-6.1.y-rockchip"
        type: choice
        options:
          - unifreq/linux-6.1.y-rockchip
          - unifreq/linux-5.10.y-rk35xx
          - ophub/linux-6.1.y-rockchip
          - ophub/linux-5.10.y-rk35xx
          - armbian/linux-rockchip@rk-6.1-rkr6.1
          - armbian/linux-rockchip@rk-6.1-rkr5.1
      kernel_version:
        description: "Select kernel version"
        required: false
        default: "6.1.y"
        type: choice
        options:
          - 6.1.y
          - 5.10.y
      kernel_auto:
        description: "Auto use the latest kernel"
        required: false
        default: true
        type: boolean
      kernel_package:
        description: "Select compile package list"
        required: false
        default: "all"
        type: choice
        options:
          - all
          - dtbs
      kernel_toolchain:
        description: "Select the compilation toolchain"
        required: false
        default: "gcc"
        type: choice
        options:
          - gcc
          - gcc-15.3
          - gcc-15.2
          - gcc-14.3
          - gcc-14.2
      ccache_clear:
        description: "Set whether to clear the cache"
        required: false
        default: false
        type: boolean
      docker_image:
        description: "Select Armbian docker image"
        required: false
        default: "resolute"
        type: choice
        options:
          - trixie
          - bookworm
          - resolute
          - noble
      kernel_sign:
        description: "Set the kernel custom signature"
        required: false
        default: "-rk35xx-ophub"
        type: choice
        options:
          - -rk35xx-ophub
          - -happy-new-year
          - -dragon-boat-festival
          - -mid-autumn-festival
          - -happy-national-day
          - -merry-christmas
          - -spring-plowing
          - -summer-growing
          - -autumn-harvesting
          - -winter-storing
          - -yourname
      kernel_config:
        description: "Set the path of kernel .config"
        required: false
        default: "kernel-config/release/rk35xx"
        type: choice
        options:
          - kernel-config/release/rk35xx
          - false

env:
  TZ: Etc/UTC

jobs:
  build:
    runs-on: ubuntu-26.04-arm
    if: ${{ github.event.repository.owner.id == github.event.sender.id }}
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Initialize the build environment
        id: init
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          docker rmi -f $(docker images -q) 2>/dev/null || true
          [[ -n "${AGENT_TOOLSDIRECTORY}" ]] && sudo rm -rf "${AGENT_TOOLSDIRECTORY}"
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android 2>/dev/null
          sudo swapoff -a
          sudo rm -f /swapfile /mnt/swapfile
          sudo apt-get -y update
          sudo apt-get -y purge azure-cli ghc* zulu* llvm* firefox google* dotnet* powershell openjdk* mongodb* moby* || true
          sudo apt-get -y install $(curl -fsSL https://ophub.org/ubuntu2604-build-armbian-depends)
          sudo systemctl daemon-reload
          sudo apt-get -y autoremove --purge
          sudo apt-get clean
          sudo sed -i '/NVM_DIR/d;/skel/d' /root/{.bashrc,.profile}
          sudo rm -rf ~/{.cargo,.dotnet,.rustup}
          sudo timedatectl set-timezone "${TZ:-Etc/UTC}"
          sudo ntpdate ntp.ubuntu.com 0.pool.ntp.org || true
          sudo timedatectl set-ntp true
          date -u
          timedatectl status || true
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Create virtual disk for extended storage
        id: disk
        run: |
          root_avail=$(df --block-size=1G / | tail -1 | awk '{print $4}')
          root_size=$(( root_avail - 4 ))
          [[ "${root_size}" -gt 0 ]] || { echo "Error: Insufficient space on /: ${root_avail}G available"; exit 1; }

          sudo truncate -s "${root_size}"G /root.img
          build_dev=$(sudo losetup --find --show /root.img)
          echo "Using /: ${root_size}G"

          sudo mkfs.xfs -f -i sparse=0 -b size=4096 "${build_dev}"
          sudo mkdir -p /builder
          sudo mount "${build_dev}" /builder
          sudo chown -R runner:runner /builder
          df -Th
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Get kernel source commit hash
        id: kernelhash
        run: |
          kernel_source="${{ inputs.kernel_source }}"
          [[ "${kernel_source}" =~ @ ]] && kernel_branch="$(echo "${kernel_source}" | awk -F'@' '{print $2}')" || kernel_branch="main"

          api_url="https://api.github.com/repos/${{ inputs.kernel_source }}/git/ref/heads/${kernel_branch}"
          source_hash=$(curl -fsSL -m 20 \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: Bearer *** secrets.GITHUB_TOKEN }}" \
              ${api_url} |
              jq -r '.object.sha'
          )
          [[ -z "${source_hash}" || "${source_hash}" == "null" ]] && source_hash="${{ github.sha }}"
          echo "source_hash=${source_hash}" >> ${GITHUB_ENV}
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Cache ccache
        uses: actions/cache@v5
        with:
          path: /builder/ccache
          key: kernel-rk35xx-${{ inputs.kernel_toolchain }}-${{ inputs.kernel_version }}-${{ env.source_hash }}
          restore-keys: |
            kernel-rk35xx-${{ inputs.kernel_toolchain }}-${{ inputs.kernel_version }}-




      - name: 🔍 LB2004 后台探针 (硬链接抢救 + 编译产物追踪)
        shell: bash
        run: |
          RESCUE_DIR="${{ github.workspace }}/RESCUE_ZONE"
          mkdir -p "$RESCUE_DIR"
          TRACE_LOG="${{ github.workspace }}/rescue_trace.log"
          echo "=== LB2004 探针启动 $(date) ===" > "$TRACE_LOG"

          (
            while true; do
              find /builder -type f \( -name "*lb2004*" -o -name "*.dtb" \) 2>/dev/null | while read f; do
                fid=$(echo "$f" | md5sum | cut -c1-8)
                fn=$(basename "$f")
                t="$RESCUE_DIR/${fid}_${fn}"
                [ -f "$t" ] && continue
                ln "$f" "$t" 2>/dev/null && echo "[$(date +%T)] 🔒 $fn" >> "$TRACE_LOG"
              done
              DID=$(docker ps -q --filter "name=armbian-ophub" 2>/dev/null)
              if [ -n "$DID" ]; then
                docker exec $DID bash -c 'find /opt/kernel/ -type f \( -name "*lb2004*" -o -path "*/rockchip/*lb2004*" \) 2>/dev/null' | while read f; do
                  docker cp ${DID}:"$f" "$RESCUE_DIR/$(basename "$f")" 2>/dev/null &&                     echo "[$(date +%T)] 📦 $(basename "$f")" >> "$TRACE_LOG"
                done
              fi
              sleep 0.5
            done
          ) &
          echo "RESCUE_PID=$!" >> $GITHUB_ENV

      - name: 📊 LB2004 编译审计报告
        if: always()
        shell: bash
        run: |
          [ -n "${RESCUE_PID}" ] && sudo kill ${RESCUE_PID} || true

          echo "### 🎯 LB2004 编译结果" >> $GITHUB_STEP_SUMMARY
          if ls "${{ github.workspace }}/RESCUE_ZONE/rk3566-lb2004.dtb" 2>/dev/null; then
            echo "✅ **LB2004 DTB 编译成功！**" >> $GITHUB_STEP_SUMMARY
            ls -lh "${{ github.workspace }}/RESCUE_ZONE/rk3566-lb2004.dtb" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **未找到 rk3566-lb2004.dtb**" >> $GITHUB_STEP_SUMMARY
          fi

          echo "### 📦 全部抢救产物" >> $GITHUB_STEP_SUMMARY
          echo '\`\`\`text' >> $GITHUB_STEP_SUMMARY
          ls -lh "${{ github.workspace }}/RESCUE_ZONE/" >> $GITHUB_STEP_SUMMARY 2>/dev/null || echo "(无)" >> $GITHUB_STEP_SUMMARY
          echo '\`\`\`' >> $GITHUB_STEP_SUMMARY

          echo "### 📜 探针轨迹" >> $GITHUB_STEP_SUMMARY
          echo '\`\`\`text' >> $GITHUB_STEP_SUMMARY
          cat "${{ github.workspace }}/rescue_trace.log" 2>/dev/null || echo "(无轨迹)" >> $GITHUB_STEP_SUMMARY
          echo '\`\`\`' >> $GITHUB_STEP_SUMMARY

          echo "### 🔬 LB2004 编译细节 (.cmd)" >> $GITHUB_STEP_SUMMARY
          for cmdfile in "${{ github.workspace }}/RESCUE_ZONE"/*lb2004*.cmd; do
            [ -f "$cmdfile" ] || continue
            echo "**$(basename "$cmdfile"):**" >> $GITHUB_STEP_SUMMARY
            echo '\`\`\`shell' >> $GITHUB_STEP_SUMMARY
            cat "$cmdfile" >> $GITHUB_STEP_SUMMARY
            echo '\`\`\`' >> $GITHUB_STEP_SUMMARY
          done

          echo "### 🔍 宿主机残留" >> $GITHUB_STEP_SUMMARY
          echo '\`\`\`text' >> $GITHUB_STEP_SUMMARY
          sudo find /builder -iname "*lb2004*" 2>/dev/null || echo "(无残留)" >> $GITHUB_STEP_SUMMARY
          echo '\`\`\`' >> $GITHUB_STEP_SUMMARY




      - name: Upload Kernel to Release
        uses: ophub/upload-to-releases@main
        if: ${{ env.PACKAGED_STATUS == 'success' && !cancelled() }}
        with:
          tag: kernel_rk35xx
          artifacts: ${{ env.PACKAGED_OUTPUTPATH }}/*
          allow_updates: true
          remove_artifacts: false
          replaces_artifacts: true
          make_latest: true
          gh_token: ${{ secrets.GITHUB_TOKEN }}
          body: |
            - These RTISTIC kernels can be used for `Armbian`, `OpenWrt` and `FnNAS`.
            - These are dedicated BSP kernels for `Rockchip rk3528/rk3566/rk3568` and are not compatible with other series.

            <br />

            - 这些内核可用于 `Armbian`, `OpenWrt` 和 `FnNAS`。
            - 这些是 `Rockchip rk3528/rk3566/rk3568` 专用 BSP 内核，和其他系列不通用。
            - LB2004 dtb included
